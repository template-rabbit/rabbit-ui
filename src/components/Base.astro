---
import ListElement from "../atom/ListElement.astro";
export interface Props {
  brand?: boolean;
}

const {brand} = Astro.props as Props;

const combined = brand && 'text-brand'
---

<div class={combined}>
  <slot />
</div>
